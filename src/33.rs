// This is a very simple example of Rust's basic syntax and features.
// For more complex examples, consider using libraries like std::io or futures.

fn main() {
    // Declare a variable to hold an integer.
    let number = 42;

    // Print the value of 'number' using println!
    println!("The value of 'number' is {}", number);
}
